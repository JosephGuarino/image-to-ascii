For best results inport an image either with a transparent background or with simple colors. Color match has higher success than edge detection.

Future
################
1. Option for multiple colors via color match
2. Text input box for specific ascii characters
3. Pre-defined choices for specific ascii characters
